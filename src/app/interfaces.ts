export interface facility {
  id: number;
  img: string;
  title: string;
  desc: string;
}
export interface location {
  id: number;
  img: string;
  title: string;
  desc: string;
  price: number;
  raiting: number;
}

export interface blogCard {
  id: number,
  img: string,
  title: string,
  desc: string,
  date: string
}